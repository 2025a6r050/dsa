#include&lt;stdio.h&gt;
#include&lt;string.h&gt;
int main(){
    char str[100];
    printf(&quot;Enter Expression: &quot;);
    gets(str);
    int len = strlen(str), top = -1, flag = 0;
    char stack[len];
    for(int i=0; i&lt;len; i++){
        char ch =  str[i];
        if(ch==&#39;(&#39; || ch==&#39;[&#39; || ch==&#39;{&#39;)
            stack[++top] = ch;
        else if(ch==&#39;)&#39; || ch==&#39;]&#39; || ch==&#39;}&#39;){
            if(ch==&#39;)&#39; &amp;&amp; stack[top]==&#39;(&#39;)
                top--;
            else if(ch==&#39;]&#39; &amp;&amp; stack[top]==&#39;[&#39;)
                top--;
            else if(ch==&#39;}&#39; &amp;&amp; stack[top]==&#39;{&#39;)
                top--;
            else{
                flag = 1;
                break;
            }
        }
    }
    if(flag == 0 &amp;&amp; top == -1)
        printf(&quot;Balanced !!!&quot;);
    else
        printf(&quot;Not Balanced!!!!&quot;);
    return 0;
}
